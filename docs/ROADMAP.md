# Orcy — Product Roadmap

> **Version:** v0.41.1 | **Updated:** 2026-08-31

Each minor release tells a story — a coherent set of changes with a clear "why."
Release boundaries are risk management decisions: breaking changes, fragile features, and big refactors never ship together.

> **Release-state convention.** "Delivered" entries describe implementation completion. The version/tag is cut by `release-it` only after an explicitly approved release; until then, an entry is described as "implementation complete; release pending" and the `package.json`/`@orcy/shared` version remains at the previous tag. "Shipped" wording is reserved for the moment the release tag lands.

---

## Delivered

| Ship | Story |
|------|-------|
| v0.1–v0.2 | Core product: kanban + MCP + Web UI + hierarchical model + Pulse V1 + rebrand |
| v0.3 | Fix foundations: shared config, ID normalization, CLI error handling |
| v0.4 | Stop type drift: shared types package, shared API client |
| v0.5 | Clean API internals: JWT extraction, schema split, scheduler, webhook dedup, rate limiter unification, board access consolidation, AppError migration |
| v0.6 | Pulse — habitat-level signals, project insights (institutional memory), signal reactions, WebUI Signal Board tab, habitat + insights panels |
| v0.7 | Solid Ground: Phase 3 UI refactors — consolidated formatting/badge utilities, fixed SSE notifications, extracted AgentCard, decomposed habitatStore (7 slices) and useTaskDetailPanel (8 hooks), fixed FilterBar auth bypass (6 of 7 planned refactors; R16 React Query unification deferred) |
| v0.8 | See the Invisible — board health metrics (0-100 composite score, A-F grade, 5 dimensions), audit log exports (streaming CSV/JSON/JSONL), feature-level comments (threaded discussion on missions) |
| v0.9 | Work Your Way — task board view (table/list with sorting, filtering, bulk ops), dynamic prioritization rules engine (10 condition types, auto-recalculates priority), recurring scheduled tasks (cron/interval/one-time, template-based feature creation) |
| v0.9.1–v0.9.4 | Patch fixes — post-release audit (20 issues), API docs restoration, R16 React Query unification (17 components), release tooling fixes, scheduled task title templating (`{{date}}`/`{{counter}}` tokens) |
| v0.10.0 | "Breaking Change" — naming consistency (`board→habitat`, `feature→mission` across 5 packages, 9 DB tables, 200 routes, 12 MCP tools, 12 SSE event types), unified `orcy_*` MCP tool prefix |
| v0.11.0 | "Guardrails" — review assignment rules (domain routing, anti-self-review, multi-reviewer approval gates), sprint/iteration management (time-boxed sprints, burndown, carry-over policies), visual rule builder (data-driven config tables with sortable cards), mobile table view (card-based responsive fallback), notification preferences (review assigned + priority changed toggles), SSE cache invalidation for all new event types |
| v0.12.0 | "Fit In" — GitHub Issues sync (OAuth device flow primary, PAT fallback), external issue intake with guarded close protection, webhook-driven updates (HMAC-verified), provider-neutral adapter framework with GitHub adapter, intake candidate system for Jira/Linear, integration settings tab with connection management, external issue badges on missions |
| v0.13.0 | "Fit In More" — Jira Cloud adapter (JQL search, ADF text extraction, API-token/Basic-Auth + OAuth 3LO), Linear adapter (GraphQL queries, cursor pagination, OAuth PKCE public-client), Intake Review UI (promote/ignore/clarify intake candidates), CLI OAuth connect + integration guide commands, shared OAuth callback infrastructure (fixed port 17530, server-side PKCE verifier store), 20 API route handlers |
| v0.14.0 | "Autonomous" — daemon & cron automation: standalone CLI daemon, in-process UI-controlled daemon engine, daemon-owned agents for Claude/Codex/OpenCode/Cursor/Gemini, worktree-backed session spawning, scheduler pulse nudges/digests, daemon status/setup UI |
| v0.15.0 | "The Habitat Remembers" — dynamic habitat skills: auto-generated living skill documents from pulse signals, task outcomes, and agent observations. Signal clustering, strength scoring, category classification (domain knowledge, conventions, patterns, anti-patterns). Hook registry for ingestion. MCP/CLI/UI integration. Skill context injected into agent mission context. |
| v0.15.1–v0.15.3 | Habitat skill hardening patches — input validation + error handling + security improvements, strength constraints + hash-based normalization for dedup, stale cross-mission count fix + FK retry loop + signal matching gap fixes. |
| v0.16.0 | "Provenance" — code ↔ task linking (8 provenance tables, 7 evidence types, append-only corrections, completeness & gap tracking, repository settings, backfill) and time tracking & effort logging (deliberate effort entries separate from inferred time, correction audit trail, quality gate split, habitat effort metrics) |
| v0.16.1–v0.16.6 | Provenance deepening patches — optimistic locking with retry for concurrent effort updates, code evidence route modularization + self-referencing FK, repository error handling + SQLite error mapping, shared CodeEvidencePanel component, effort tracking metrics + database indexes, repository import/type cleanup. |
| v0.17.0 | "Evidence" — Audit Trail V2 (canonical, provenance-aware audit projection across lifecycle, effort, code evidence, pipeline, integration, webhook, and opt-in health snapshot sources; scoped task/mission evidence bundles; integrity-ready archival) and Advanced Analytics (confidence-aware forecasting, trend analysis, cumulative-flow snapshots, bottleneck detection, sprint analytics, and informational-only agent quality signals) |
| v0.17.1 | "Deepen: Transition Core" — TransitionEmitter deep module consolidating the 5-layer task transition side-effect chain (DB write → event → SSE → watcher → mission recalc) into one `emitTransition` seam across 5 caller files (16 actions, opt-in mission recalc debounce); and API Client Domain Split defining 23 per-domain interfaces with `KanbanApiClient` as the typed facade, per-domain mock factories (33% → 100% method coverage), and `getMissionContext` extracted to a standalone orchestrator service |
| v0.17.2 | "Tighten: Effort and Notification Plumbing" — effort metrics recompute on complete/approve (FU-001 correctness fix), `NOTIFY_TASK_EVENT_ACTIONS` consumer audit constant, 13 MCP handler files narrowed to per-domain interfaces, `ORCY_TRANSITION_RECALC_DEBOUNCE` env documentation, 194 `@requires` JSDoc tags across 25 MCP tool files |
| v0.17.3 | "Prep: Event Spine" — SSE Event Registry: centralized event handler pattern replacing the triple-switch (Zustand mutation, React Query invalidation, toast/dropdown notification) across all SSE event types. New events registered once and covered by completeness tests. Foundation that v0.18 automation and Notification V2 event types build on. |
| v0.18.0 | "Rules That Act" — Workflow Automation Engine (12 trigger types, 9 action types, condition evaluation with AND/OR/NOT nesting, simulation, cooldown/fingerprint/rate-limit guards, scheduled scans), Notification System V2 (subscriptions, channel routing, in-app/webhook/Slack/Discord delivery, acknowledgment/snooze/mute, digests with hourly/daily/weekly cadence, retention-based clearance), MCP self-service surfaces (read-only automation, self-service notifications), UI automation settings tab, REST routes, audit projection |
| v0.18.1 | "Deepen: Data Access" — 64 direct `getDb()` calls eliminated across 11 target services, 6 new repository files, `dependencyService`/`anomalyService`/`auditExportService`/`predictionService`/`notificationService`/`syncService`/`webhook-subscriptions`/`webhook-delivery`/`webhook-dispatch`/`auditArchivalService`/`sprintService`/`boardHealthService` all at zero `getDb()`, `task.ts` and `daemon.ts` repository splits, 4 v0.18 review fixes |
| v0.18.2 | "Deepen: Route Extraction" — Pulse posting handlers (2×110 lines → 17 each), intake candidate promotion (55 lines → 14), daemon register + claim-next (2×65 lines → 12 each), Jira/Linear OAuth completion flows, auth register/me/setup extraction, mission progress deduplication |
| v0.18.3 | "Deepen: Single Cache" — Zustand → React Query for all server data (agents, comments, missions, board/columns, tasks). 5 mutation hooks de-dual-written. 14 SSE zustand blocks removed. Zustand now holds only ephemeral UI state (modals, theme, presence, pagination) |
| v0.19.0 | "Pod Bridge" — Remote participant identity (external identity providers, pod trust model, participant standing), scoped habitat access (grants, credentials, invite flows), Shared Habitat API (`/api/shared/*` — discovery, missions, tasks, comments, pulse, evidence, notifications, trust metadata), remote MCP mode (action allowlist, `X-Orcy-Remote-Key` auth), idempotent write contracts, admin surface (readiness checks, provider config, grant management, webhook endpoints), audit provenance (remote actor labels, provenance block, export filters), UI management surface (Remote Pods page, inline attribution) |
| v0.19.1 | "Deepen: API → Daemon Interface Seam" — Shared daemon types (`SessionStatus`, `ClaimResult`, `DetectedCli`, `RegisteredAgent`, `ActiveSession`, `ISessionUpdater`, `WorkdirError`) moved to `@orcy/shared`. Six seam interfaces (`ISessionManager`, `ISessionUpdater`, `ICliDetector`, `IClaimStrategy`, `IHeartbeatStrategy`, `IPollLoop`) defined in shared. `runPollTick` consolidated pure async function replacing duplicated tick loops in daemon and API. `InProcessClaimStrategy` + `HttpClaimStrategy` strategy classes. API's `daemon-wiring.ts` DI module with dynamic import. Zod schemas derived from `AGENT_TYPES` runtime array. `@orcy/daemon` moved to devDependencies. 54 new tests (interface-compliance + seam + poll + factory + wiring) |
| v0.19.2 | "Deepen: Documentation Pass" — CONFIGURATION.md updated with 17 missing env vars, 3 stale removed, JWT_SECRET security doc bug fixed. ARCHITECTURE.md gained Daemon Runtime Seam + Audit Trail V2 sections. DATABASE.md gained 8 automation/notification table entries. 28 JSDoc blocks added to daemon seam public APIs. TESTING.md gained 5 test pattern sections + UI test count fix. TROUBLESHOOTING.md gained 8 entries across security, remote pods, notifications, daemon. README/CAPABILITIES/SKILL refreshed: MCP count 15→16, Pod Bridge features added, dispatch tools completed |
| v0.19.3 | "Deepen: Inline JSDoc Pass" — Comprehensive inline JSDoc coverage across all 6 packages. Shared types (245 symbols across 18 files), API services (~600 symbols across 130+ files including tasks/, webhooks/, integrations/, code evidence, notifications, automation, audit, and core services), MCP dispatch handlers and tools (~310 symbols), daemon runtime (~60 symbols), and CLI commands (16 symbols). Every exported symbol now has an IDE-visible description. Pod Bridge domain types restored with full design rationale and scope notes. |
| v0.20.0 | "Orchestrated" — Mission-scoped workflow DAGs with typed gates (`on_complete`, `on_approve`, `on_signal`, `on_manual`, `on_fail`), `all_of`/`any_of`/`n_of` join specs, and conditional edge predicates reusing v0.18 AutomationCondition. Workflow gates layer on the existing claim path as derived constraints — no new task status, no changes to IClaimStrategy or runPollTick. `on_fail` gates spawn recovery tasks with structured FailureContext (artifacts, lifecycle events, experience signals, retry history); successful recovery redeems the original failure and downstream gates fire as if the original had succeeded. Two recovery attempts maximum. Agent experience self-reporting via `orcy_pulse` with `signalType: "experience"` and 7 categories (`stuck\|confused\|backtrack\|surprised\|ambiguous\|sidetracked\|smooth`); signals flow through the existing pulse pipeline into habitat skills and failure contexts. Workflow templates extend `missionTemplates` with a `workflowTemplate` JSON column; two default templates shipped (Build-Test-Review-Deploy, Parallel Investigation). Form-based UI editor with JSON import/export, live SVG preview, workflow DAG visualization on mission detail page, blocked-by-workflow filter, admin metrics dashboard, and cross-pod read-only workflow context routes. |
| v0.20.1 | "Orchestration Patch" — Wired `automationExecutor.executeActions` into production via `executeAndRecordRuleRun` (consolidated lifecycle: start run → kill-switch check → execute actions → finish run → notify hooks). All event + scan paths now actually fire actions. Added `on_automation` gate type (6th gate type) with `onAutomationRunCompleted` subscriber hook + workflow service gate evaluation + AutomationMatch form fields in workflow editor. Habitat-scoped kill switch (`automationSettings.executeActions`) toggleable via UI, CLI, and env var. Added `anti_patterns` to `SkillCategory` (consolidated 6 duplicate definitions to `@orcy/shared`), remapped `sidetracked → anti_patterns`, separate bucket in skill document generation. |
| v0.20.2 | "Deepen: Trim Pass-Throughs" — Deleted dead-code `task-movement.ts` (`moveTask`/`reorderTask`, zero production callers) and misleading aliases `assembleHabitatContext`/`assembleCrossHabitatDependencies` in `task-details.ts` (zero import sites). Inlined 3 `watcherService` pass-throughs (`unwatchTask`, `isWatching`, `getWatchers`) directly into their route and service callers via `watcherRepo`. `watcherService` now contains only `watchTask` (validation logic) and `notifyWatchers` (SSE logic). MCP forwarding-handler auto-generation deferred to later patch. |
| v0.20.3 | Test DB snapshot caching optimization, package license update, documentation reorganization. |
| v0.21.0 | "Living Library" — Authored, versioned, searchable Habitat Wiki with tree hierarchy, collection tags, append-only version history, FTS5 full-text search, polymorphic citations with read-time dangling detection (ADR-0007), and a scheduler-driven cadence that spawns authoring tasks (ADR-0008). Coverage watermark with two-mode deletion + no-update-needed markers (ADR-0009). Authoring augmentation surface exposing primitives as authoring context (delta-on-edit, chunk-on-create). Implicit Signal Surfacing: Experience Signals tab (aggregated-only, privacy-protected) and Engineering Findings tab (structured metadata convention with layered Zod opt-in validation, ADR-0010). `orcy_wiki` MCP tool with 13 actions. 4 wiki tables + FTS5 virtual table + 2 migrations. 5 ADRs (0006-0010). |
| v0.21.1–v0.21.6 | Living Library fix patches — strip experience-signal source IDs from authoring context (privacy), isolate wiki graph per habitat, correct coverage watermark windows + wire cadence execution, bound authoring-context chunk queries to requested window, align client contracts + complete SSE cache invalidation, explicit handler-key dispatch with fail-loud guard + extract `parseDurationWindow`. |
| v0.22.0 | "Ecosystem" — Plugin System V2: plugin manifest contract with 5 contribution kinds (`signalDetector`, `notificationChannel`, `lifecycleInterceptor`, `customMcpTool`, `customHttpRoute`), manifest/module split, 5-capability whitelist, per-habitat enrollment with detector allowlist (`ORCY_DETECTOR_ALLOWLIST`), in-memory auto-quarantine. Lifecycle interceptors with pre-veto + post-emit phases (ADR-0014). Custom signal detector plugins writing `signalType:"detected"` (11th category, ADR-0013) via trigger-based fire-and-forget-after-commit execution (ADR-0015). Notification channel registry surface — `teams-channel` reference plugin shipped; existing in-tree channels untouched (ADR-0017). `customMcpTool` validated-only in v0.22.0 — dispatch wiring deferred to v0.22.1 (ADR-0018). 2 new DB tables (`plugin_enrollments`, `plugin_runs`); audit source `"plugin"`. 3 reference plugins: `auto-label` (rewritten as lifecycleInterceptor), `detector-regex-frustration`, `teams-channel`. 8 ADRs (0011-0018). |
| v0.22.1 | bump typescript to 6.0.3 |
| v0.22.2 | "Ecosystem Fixes" — widen `NotificationChannel` type from closed 4-literal union to `string` (teams-channel reference plugin goes operationally live end-to-end), widen 2 Zod subscription validators to accept plugin-registered channels, re-mount `GET /plugins` under `/api/plugins` (UI `request()` 404 fix), fix audit source enumeration drift across 3 files (`AUDIT_SOURCES` / `sourceFromAuditMetadata` / `isAuditSource` missing `automation`/`notification`/`workflow` — production events were coerced to `"unknown"` in cross-source audit views), delete dead `webhookUrl` cast-hack in `dispatchChannel`. |
| v0.22.3 | "Detection Hardening" — per-contribution `timeoutMs` watchdog wrapping all async plugin handler calls in `Promise.race` (detectors default 5000ms; channels/post-interceptors default 0/disabled; manifest-declared overrides defaults; on timeout → error counter increments toward auto-quarantine). Detector catch-up scan recovering events missed during server downtime via `lastScannedAt` watermark on `plugin_enrollments` + periodic `detectorScanService` with `plugin_runs` dedup check (prevents duplicate signals on re-scan; `ORCY_DETECTOR_SCAN_INTERVAL_SECONDS` default 300). Persistent `plugin_quarantines` table surviving API restart + `loadQuarantinesFromDb()` boot re-population + admin `DELETE /habitats/:id/plugins/:pluginKey/quarantine` clear endpoint. 2 migrations (`0040`, `0041`). |
| v0.22.4 | "Plugin Review Fixes: Runtime & Pipeline" — quarantine key mismatch fix (`incrementError` now uses composite `"pluginId:detectorId"` key matching `dispatchDetectionEvent`'s quarantine check — quarantine was functionally broken since v0.22.0). Pre-interceptor run-leak fix (deleted dead `startRun`+`cryptoRandom` from `runPreInterceptors` that leaked `"running"` rows forever + used phantom runId). Catch-up scan watermark fix (only advances when all events dispatched or deduped — previously advanced past events silently dropped by quarantine/rate-limit/concurrency cap). `detected_patterns` section added to `generateSkillDocument` (promoted detected signals were invisible in skill output since v0.22.0). `reclassifyCategory` guard (detected signals were promoted to `pattern`, erasing ADR-0013 provenance distinction). Plus 9 warnings: dead code deletion (`loadEnrollmentCacheForAllHabitats`), `orphanHandler` typeof-function check, within-manifest duplicate detection, `errorCounters`/`activeRuns` GC, `detectedMetadataSchema` enforcement, MCP `signalType:"detected"` rejection, misleading `enabled` field removed from `PluginManifestView`, misleading `ACTION_EFFECTS` comments fixed. |
| v0.22.5 | "Plugin Review Fixes: Security & Wiring" — cross-tenant reader leak fix (all 4 capability readers now scope to the contribution's `habitatId` — plugins in habitat A can no longer read habitat B's pulses, tasks, comments, or habitat data; `getTask`/`getPulse` verify habitat ownership before returning). `completeTask` pre-interceptor moved BEFORE irreversible DB writes (was after 4 writes — metrics, effort, artifacts, review note — violating ADR-0014). `/api/plugins` route moved into `pluginRoutes` with `agentOrHumanAuth` (was unauthenticated on root mount, leaking plugin IDs + error messages). Dead `onTaskEvent` hook bus deleted from transition-emitter (task-lifecycle's copy is canonical; transition-emitter's was firing on an empty array with no subscribers). Boot order fix (`loadQuarantinesFromDb` moved before `loadPlugins`). `createTask` always calls interceptor seams (was silently skipping when `habitatId` empty). teams-channel error messages sanitized (was leaking webhook hostname via fetch errors). `deleteEnrollment` return value surfaced to API. Plus quarantine clear scope documented as system-global by design. |
| v0.22.6 | "Channel Migration" — ADR-0019 adds `chatIntegrationReader` capability (6th on PluginContext whitelist) enabling notification channel plugins to resolve per-habitat webhook URLs. 4 channel plugin modules created (`channel-in-app`, `channel-webhook`, `channel-slack`, `channel-discord`) as thin wrappers calling existing `deliverXxx` functions. `NotificationChannelContribution.requires` widened from `[]` to `PluginCapabilityName[]`. `ChatIntegrationView` strips `botToken` (security). Gradual migration: hardcoded switch in `dispatchChannel` retained as backward-compat fallback. |
| v0.22.7 | "Loose Ends" — final cleanup of TIER 3 deferred warnings from the v0.22 code review. Path traversal guard in `loadPlugins` (realpath check prevents symlinked entries from importing code outside `PLUGINS_DIR`). `withTimeout` late-rejection suppressor (prevents `unhandledRejection` when timeout wins the race). `createDetectedSignal` throws on null `habitatId` (was silently creating invalid FK rows). `createPulseAndNotify` broadcast contract documented (JSDoc). Catch-up scan task event sourceId now includes action type (`taskId:action` format). `InterceptorVeto.details` widened from `string` to `string  Record<string, unknown>`. |
| v0.22.8 | "Plugin Foundation" — Data-driven `CAPABILITY_MATRIX` (replaces if/else chain, adding contribution kinds becomes data entry). `startPluginRun` utility extracted from 3 dispatchers. `taskWriter` write capability (ADR-0020) shipped dormant — unreachable until v0.22.11 wires it into `automationAction` contribution kind. |
| v0.22.9 | "Webhook Formatters" — `webhookFormatter` 6th contribution kind (ADR-0021). Plugin-first formatter dispatch with in-tree fallback. 3 reference plugins (`formatter-standard`, `-slack`, `-discord`). First proof-of-concept for the v0.22.8 foundation. |
| v0.22.10 | "Automation Condition Plugins" — `automationCondition` 7th contribution kind (ADR-0022). Synchronous leaf nodes in condition tree; fail-safe dispatch (errors → `{matched:false}`). `PluginEvaluationContext` view. `condition-rejection-spike` reference plugin. |
| v0.22.11 | "Automation Action Plugins" — `automationAction` 8th contribution kind (ADR-0023). Activates `taskWriter` (dormant since v0.22.8); new `notificationSender` + `webhookCaller` capabilities (with SSRF guard). `dispatchActionHandler` with full run tracking + quarantine. `action-create-followup` reference plugin. Plugin extraction arc completes: 8 kinds, 9 capabilities. |
| v0.22.12 | "Plugin Extraction Review Fixes" — shared write-cap counter (was 150 total, now 50/run per ADR-0020). Action quarantine (`dispatchActionHandler` now calls `incrementError` on failure). `PluginRun.contributionKind` widened to include `automationAction`. `validateUrl` SSRF cleanup. |
| v0.22.13–v0.22.15 | Cleanup patches — optional `scheduleType` in route schemas, `AgentAvatar` component consolidation (replaces 3 inline impls) + a11y improvements (`aria-label`s, sorting indicators), `useSSE` null/missing `taskId` edge-case tests + sprint event detail-query invalidation tests. |
| v0.23.0 | "Triage" — Automated detection and response to systemic agent pain points. Reactive Triage: periodic `signal_pattern_clustered` scan groups time-windowed pulses by normalized subject across experience/finding/detected provenance (ADR-0024/0025), fires automation rules per-cluster with typed `ClusterPayload`, creates triage missions via template instantiation with embedded cluster context + active-triage suppression via `triage_cluster_missions` junction (ADR-0026). Proactive Triage: `triage_resolutions` table records how patterns were resolved (root cause, fix, kind), keyed by clusterKey for historical surfacing — new clusters with matching keys get suggestions attached before triage. Agent Quality Triggers: periodic `agent_quality_degraded` scan evaluates per-agent composite quality scores against configurable threshold with sample-size gate; informational only (no assignment/gate/permission mutation per CONTEXT.md). Implementation-Finding Triage: parallel `finding_triage` table with 5-state lifecycle (open→triaged→in_progress→resolved/wontfix), routing buckets (fix_now/defer_to_patch/defer_to_release/document_as_known_limitation/needs_investigation), dedup contract (clusterKey+findingKind+affectedFiles), bidirectional pulse linkage (ADR-0027). Bucket decisions human-in-the-loop. `executeAndRecordRuleRun` gains optional backward-compatible payload parameter for per-cluster trigger context. `orcy_triage` MCP dispatch tool (investigate, top_issues, resolution_lookup). Source-tagged analysis pulses (`metadata.triageGenerated`) + active-triage suppression = two-layer loop prevention. Triage investigation runs as daemon-owned task (no server-side LLM). 3 new tables, 3 migrations (0042–0044), 4 ADRs (0024–0027), 6 CONTEXT.md glossary terms. |
| v0.23.1 | "Integration Adapter Surface" — Extract GitHub/Jira/Linear issue-provider adapters into `plugins/integration-{github,jira,linear}/` reference plugins behind a new `integrationProvider` contribution kind (ADR-0028). `providerRegistry` + `getProviderAdapter()` lookup in `pluginManager`, consulted first by `getAdapter()` with the existing dynamic `require()` retained as backward-compat fallback (gradual migration per ADR-0017). Adapter-only scope: OAuth, webhook ingestion, webhook creation stay in-tree (credential management + unauthenticated signature-verified routes deferred to future security-gated ADR). 3 reference plugins wrap the in-tree adapter objects (channel-plugin pattern). Plugin contribution kind count 8→9. |
| v0.23.2 | "Triage Hardening" — Adds missing habitat membership checks (`verifyHabitatAccess()`) to all 6 triage endpoints that shipped without authorization in v0.23.0 (cross-tenant read/write vulnerability fix). Also introduces `findActiveClusterKeys()` batch query to eliminate N+1 per-cluster queries in `/triage/clusters/top`. |
| v0.23.3 | "Triage Settings Backend Wiring" — `triageSettings` JSON column on habitats (migration 0045), `TriageSettings` type in `@orcy/shared`, `triageSettingsSchema` in `updateHabitatSchema`. Both scan services (`triageScanService`, `agentQualityScanService`) now resolve habitat-level thresholds via `resolveThresholds()` with fallback to `DEFAULT_TRIAGE_SETTINGS`. `TriageSettingsTab` migrated from localStorage to backend persistence via `PATCH /habitats/:id`; wired into `HabitatSettingsDialog` as a "Triage" tab. |
| v0.23.4 | "Deferred Items" — `TriageActorType` lifted to `@orcy/shared` (CS-22); `targetRelease` exposed on `PATCH /triage/findings/:id` + UI `BucketConfirmation` target-version capture (v0.24.0 prerequisite); `escapeFtsQuery` fixed for multi-word FTS5 search — splits on whitespace into implicit AND instead of literal phrase match (SU-1); `writeFindingTriageIdPointer` converted from read-modify-write to atomic `json_set` + `json_extract IS NULL` guard (CS-21). Deferred ADRs 001 (round-robin persistence) and 002 (mobile PWA) formally accepted. |
| v0.23.5 | "Security Patch" — Webhook signature bypass fix (`webhookService.ts:89` — `if (signature && !verify...)` changed to `if (!signature \|\| !verify...)` so missing `x-hub-signature-256` header is rejected when secret is configured). Missing-signature test added. Triage route HTTP auth tests (8 tests covering `verifyHabitatAccess` on all 6 endpoints — anonymous → 401, agent-auth → 200/404, human-auth → 200). |
| v0.23.6 | "Triage UI Fixes" — `/triage/clusters/top` aggregation now uses `findByHabitatInStatus()` (no limit, SQL-side status filter) instead of `findByHabitat()` (limit 100) then JS filter — fixes incorrect cluster counts for habitats with >100 findings. `TriageSettingsTab` now resyncs form values on habitat switch via `useEffect`. `BucketConfirmation` sends `targetRelease: null` on non-defer re-triage (clears stale target). `usePromoteFinding` invalidates missions list after promotion. UI `transitionFinding` body type includes `targetRelease`. `DeferredBacklog` shows loading state instead of empty-state flash. TriageSettingsTab UI tests added (5 tests). |
| v0.23.7 | "Triage Data Layer Hardening" — `promote()` now delegates to `transitionStatus()` (state machine enforcement) + atomic `json_set` for `promotedAt` metadata (CS-21 pattern). `getAdapter()` has explicit `IssueProviderAdapter` return type + normalizes plugin handler with `provider` field. `POST /triage/findings/:id/promote` returns `{ finding, missionId }` instead of bare `{ missionId }`. `syncConnection` throws `AppError` (`notFound`/`badRequest`) instead of plain `Error`. `resolveImportColumn` hoisted to once-per-sync-run. `corroboratingPulseIds` append is now atomic via `json_each` + `json_insert`. Partial unique index on `triage_cluster_missions(habitat_id, cluster_key) WHERE status='open'` (migration 0046) — `create()` is idempotent on constraint hit. |
| v0.23.8 | "Triage Real-Time + MCP Enrichment" — Two new SSE event types (`triage.finding_created`, `triage.finding_updated`) emitted from finding triage creation, PATCH, and promote paths. UI registry handlers invalidate `queryKeys.triage.all` on receipt — triage views now update in real-time without staleTime polling. MCP `investigate` action response enriched with `pulseId`, `clusterKey`, `corroboratingPulseIds`, and `clusterMissionId` — agents now have the signal subject and corroboration chain needed to investigate without raw REST calls. |
| v0.24.0 | "Cadence" — Release shipping as a first-class automation trigger. Provider-agnostic release detection (`POST /triage/release-trigger` fed by GitHub `release` webhooks, GitHub Actions `workflow_run` release-workflow completion, CLI, or external systems) classifies each release by semver type via server-side diff against a new `releases` tracking table (caller-override allowed; first release per habitat requires explicit declaration; idempotent on habitat+version incl. concurrent-webhook UNIQUE catch). Semver-targeted deferrals: a new `targetReleaseType` (patch/minor/major) column on `finding_triage` plus the existing free-text `targetRelease` for version pins, with either-match (OR) semantics and a cascading-type matcher (patch ⊂ minor ⊂ major). Unconditional auto-promotion — every release-matched finding promotes into a corrective mission regardless of release type, no human gate (the human's decision point is deferral time, not release time); a two-layer kill switch (`ORCY_RELEASE_AUTO_PROMOTE` env + habitat `releaseSettings.autoPromote`) gates only the promotion loop. A source-tagged release retrospective pulse and a new `release.shipped` automation event fire on every detection; a `release.activated` notification enqueues to habitat team members. Pure semver engine in `@orcy/shared`. 5 ADRs would be expected but 3 shipped (0029–0031); the planning seed's proposed human-in-the-loop gate for minor/major was rejected (ADR-0031 — rebuilds v0.23's manual promote, defeats the feature's purpose). |
| v0.25.0 | "Roadmap Activation" — Elevates the mission dependency DAG from a passive blocker into the habitat's canonical roadmap. The DAG *is* the roadmap; no separate entity (ADR-0032). Three composing changes: (1) **release-gates** — missions gain optional `releaseGateType`/`releaseGateVersion` columns acting as hard blocking conditions in `getAvailableTasksForAgent`, resolved derive-at-read-time from the `releases` table; (2) **triage-agent insertion** — the triage investigation creates gated missions positioned in the DAG at deferral time (ADR-0033), bootstrapping the roadmap through the existing triage flow; `GET /habitats/:id/roadmap` exposes the DAG to the agent; `insert_deferred_mission` MCP action creates + links; (3) **roadmap-aware guidance** — the dead `dependencyBonus` placeholder becomes a batched fan-out computation directing agents toward work that unblocks the most downstream tasks. `detectAndActivate` resolves gates BEFORE the legacy finding-promotion loop (superseded v0.24.0's free-floating `targetReleaseType` path — greenfield, no migration). Human mission form gains a release-gate selector; gated missions show a lock badge. 2 ADRs (0032–0033), 2 CONTEXT.md terms (Roadmap, Release Gate). Deferred to patch (RM-1..14, `docs/plans/v25/PATCHES.md`): release-deadline "before" + compound window, alternative scoring algorithms + user-selectable setting, orphan-mission auto-mapping, lax UI mode, `findByTriageMissionId` N:1 safety, `isGateSatisfied` extraction, finding-mission unlink, `requireHabitat()` systemic authz, `findReleaseMatched` cleanup, mission edit form, triageInvestigate payload size. |
| v0.25.1–v0.25.8 | "Roadmap Activation patches" — the full RM-1..15 cadence plus final fast-follow delivered across 8 patch releases. v0.25.1: gate-logic extraction to `@orcy/shared` + N:1 finding-mission safety + unlink + legacy `findReleaseMatched` cleanup. v0.25.2: `requireHabitat`→`requireHabitatAccess` authz hardening on mission/plugin routes. v0.25.3: release-deadline "before" gate (escalation-on-miss, no claim-blocking) + compound release window; new `release.deadline_missed` notification. v0.25.4: selectable scoring (fanout/depth-from-root/release-proximity) via `roadmap_settings`; strategy-dispatch refactor. v0.25.5: mission edit form + feature-based authoring mode (`roadmapSettings.mode`). v0.25.6: orphan-mission scan → triage agent positions disconnected work via `map_orphan_mission`; `triageInvestigate` summary mode (payload bound). v0.25.7: goal-directed scoring toward a focus mission (explicit or self-derived highest-fan-out; soft-boost-not-gate, ADR-0034). v0.25.8: per-release promotion cap, agent `set_focus_mission` MCP action, and mission-id route authz hardening. 1 ADR (0034). |
| v0.26.0 | "Deepen: Workflow Gate Core" — Architecture-deepening release that extracts Workflow Gate DB lookups and idempotent satisfaction updates into an internal `WorkflowGateStore` module (active-gate lookup, idempotent satisfaction, manual unblock persistence) and pure trigger matching into `WorkflowGateEvaluator` (lifecycle/Pulse Signal/Automation Run matching with injected condition checker). The three trigger handlers (`handleTransition`, `handlePulseCreated`, `handleAutomationRunCompleted`) and `manualUnblockGate` delegate to these internal seams while `workflowService.ts` remains the external service boundary. Preserves WHERE-clause asymmetry (lifecycle does not pre-filter satisfied; Pulse/Automation do), condition-evaluation asymmetry (Automation Run gates do not evaluate conditions), and Recovery Redemption independence. 4 characterization tests added (detached-workflow gate suppression, automation scope matching). 28 evaluator unit tests. No schema changes, no route changes, no ADRs, no CONTEXT.md terms. |
| v0.27.0 | "Deepen: UI API Locality" — Architecture-deepening release that extracts transport helpers (`request`, `requestBlob`, `uploadFile`) into `api/transport.ts` and moves all 42 API namespace implementations from the 2,229-line `api/index.ts` into real per-domain modules under `api/domains/`. `api/index.ts` becomes a 96-line pure composition surface importing domain APIs. Adds 6 missing domain modules (`automation`, `metrics`, `notificationsV2`, `plugins`, `remoteAccess`, `workflows`). Moves top-level `myTeams()` into `teamsApi.myTeams()` with `api.myTeams` compatibility alias. Replaces alias-identity tests with method-shape compatibility tests (85 tests), independent method-name snapshots for 5 representative domains, and a direct `reviewersApi.list` behavior test. 15 transport behavior tests covering auth header injection, JSON content-type defaults, SSE base-path handling, 204 responses, error parsing, blob download, and XHR upload paths (success/error/abort/progress). Full UI suite green: 1533 tests across 126 files. No schema changes, no route changes, no ADRs, no CONTEXT.md terms. |
| v0.27.1 | "Deepen: UI API Locality patches" — removed the unused `SSE_BASE` transport constant (CS-22, deferred from v0.27.0); closed deferred test gaps TG-1 and TG-2. |
| v0.27.2 | "Webhook Task-Resolution Fix" — latent `getSettingsForHabitat` bug (`githubWebhook.ts`/`gitlabWebhook.ts` read snake_case `code_review_settings` via an erasing cast while `getHabitatById` returns drizzle camelCase) silently disabled PR/MR task-linking since the initial commit. Mirrors the already-correct CI/CD path. Adds shared Zod webhook-settings schemas + `PublicHabitat`/`PublicCodeReviewSettings`/`PublicCiCdSettings` types in `@orcy/shared`, repo allowlist + `UpdateHabitatInput`, `getHabitatWithColumnsAndTasks` drizzle-relational→plain-selects fix, write-only `PUT /habitats/:id/webhook-secrets` endpoint, `maskSecretSettings` at all 6 habitat-returning boundaries, PATCH merge semantics (PATCH no longer clobbers PUT-set secrets), integration tests + DEPLOYMENT.md correction. |
| v0.28.0 | "Deepen: Plugin Contribution Runtime" — Architecture-deepening release introducing a `ContributionAdapterCatalog` (`packages/api/src/plugins/contributionAdapters.ts`) that localizes contribution-kind registration behavior. Collapses the four kind-switches in `pluginManager.ts` (`contributionLabel`, `orphanHandler`, `detectIdCollisions`, `registerContributions`) into `CATALOG[c.kind]` lookups; the catalog owns collision error formatting and the cross-registry check via a per-adapter `collisions` sub-object (factory templates handle the lifecycleInterceptor within-suffix and the signalDetector no-suffix asymmetries). `findContribution` in `pluginEnrollmentService` folds to a single catalog-label lookup, fixing a latent gap where 4 kinds returned "not found" instead of the "cannot enroll system-scoped" error. `pluginManager.ts`: 1180 → 988 lines. 88 characterization tests pin registration (9 kinds × 4 behaviors), the dispatch contract + quarantine chain, and the generic dispatch guards (rate-limit, concurrency, `withTimeout`) via indirect observation (no test seam). Dispatch functions and `DEFAULT_TIMEOUT_MS` are byte-for-byte unchanged. The shared dispatch skeleton (`dispatchPluginRun`) is deferred — its characterization prerequisite is now met; deferred because the 5 dispatch paths have genuinely asymmetric fail-open/fail-safe policy (detectors quarantine+skip; actions quarantine but do NOT skip — a documented asymmetry; channels do not quarantine; post-interceptor rethrows; pre-interceptor is synchronous/fail-open). No schema changes, no route changes, no ADRs, no CONTEXT.md terms. |
| v0.29.0 | "Deepen: Audit Projection Internals" — Architecture-deepening release extracting Audit Trail V2 source projection into a 9-collector catalog (`lifecycle`, `effort`, `code_evidence`, `integration_sync`, `webhook_delivery`, `health_snapshot`, `automation_run`, `notification`, `plugin_run`) with `assertCatalogCoverage()` enforcing one-to-one source-family coverage of `AUDIT_QUERY_ENTITY_TYPES`. Shared `AuditSource`/`AuditQueryEntityType` move to const arrays (`AUDIT_SOURCES`, `AUDIT_ENTITY_TYPES`, `AUDIT_QUERY_ENTITY_TYPES`, `DEFAULT_AUDIT_QUERY_ENTITY_TYPES`) — eliminates literal-list drift across 9 import sites. Operational projectors (automation run, notification event/delivery, plugin run) rewritten with typed `automation`/`notification`/`plugin` provenance namespaces and metadata allowlists (no raw payloads, error text, or fingerprints) — replaces 3 `as unknown as AuditEvent` casts. Notification collector + Plugin collector deliver completeness `complete` (legacy `partial` branch removed). Notification event/delivery projectors resolve `(sourceType, sourceId)` and `(targetType, targetId)` to task/mission `linkedEntities` within the habitat; automation runs resolve `(targetType, targetId)` the same way; deliveries do NOT carry linkedEntities (excluded from Evidence Bundles — recipient data stays habitat-audit-only); plugin `triggerEventId` stays opaque. Bundle queries (`getTaskAuditBundle`, `getMissionAuditBundle`) pass `referencedEntities` to scope BEFORE pagination. `getAuditSummary` rewritten to aggregate through `collectAuditProjection` — drops `getAuditSummaryRows` (`repositories/auditExport.ts`) and adds additive `warnings` + `completenessSummary` fields. Archive isolation preserved: `extractProjectedSourceId` prefix match (`task_event:`/`mission_event:`) only deletes lifecycle source rows; operational source rows survive. Export validators centralize on `AUDIT_SOURCES` / `AUDIT_QUERY_ENTITY_TYPES`. `auditQueryService.ts` reduced to ~95 lines, re-exporting `collectAuditProjection` / `normalizeFilters` / `matchesFilters` / `sortEvents` as the single audit-surface seam. Notification collector delivery counting uses `Map<eventId, deliveries[]>` (was O(N×M) `filter`). 18-test `auditQueryCharacterization` byte-equality gate still green. |
| v0.30.0 | "Deepen: Plugin Invocation Policy" — Architecture-deepening release (ADR-0039) that consolidates the five managed plugin contribution kinds (Signal Detector, Automation Action, Notification Channel, pre/post Lifecycle Interceptor) behind one **Plugin Invocation Runtime** module (`invocationRuntime.ts`) with two entry points (`checkPreVeto` synchronous, `invokeManaged` asynchronous) and a target-specific Detector dispatch seam (`dispatchDetectorTarget`). The runtime owns startRun, defensive quarantine gate, context construction, watchdog timeout, result validation, fault classification, and finishRun; thin adapters map runtime outcomes to existing caller result shapes. Settles 15 grilling decisions (Q1–Q15) and reconciles ADR-0014/0016 drift: **bounded fail-closed pre-interceptors** (throw/invalid/synchronous-Promise = failure veto → 403, counts toward quarantine; explicit `{allow:false}` = ordinary veto), **kind-specific fault accounting** (Detectors/Actions/pre-Interceptors are quarantine-accounted; Channels/post-Interceptors are defensive-only), **canonical kind-safe contribution identity** (`(pluginId, kind, contributionId[, phase, event])` JSON-encoded by one encoder), **status-aware Detector dedup** (only `running`/`succeeded`/`failed` count as durably accounted; `skipped`/`rate_limited` remain recovery-eligible), **per-target Detector dispatch with durable-start watermark acknowledgement**, **capacity-only `rate_limited`** (the error-rate `isRateLimited` gate is removed; `rate_limited` is written solely when a Detector cannot acquire habitat concurrency capacity), **watchdog-not-cancellation concurrency slot release** (slot held until the underlying handler Promise settles, not the watchdog timeout — a never-settling handler holds its slot until process restart), **atomic transactional post-interceptor signal batch** (validate-all → one DB transaction → publish SSE after commit; replaces the sequential partial-write loop), and **final-approval pre-veto ordering** (pre-veto runs before final reviewer-approval persistence, `task.review_completed` SSE, or any other veto-gated Task-domain mutation). **One-time prerelease quarantine reset** (migration `0053`, frozen/immutable — its bytes must never be edited): legacy `plugin_quarantines` rows (ambiguous `pluginId:contributionId` format) are deleted, intentionally clearing prerelease quarantine fault-state; the canonical key format cannot map to old rows. Dead code collapse: `startPluginRun`, `cryptoRandom`, `DEFAULT_TIMEOUT_MS`, and the `ORCY_DETECTOR_QUEUE_MAX` overflow-queue path are removed (`withTimeout` retained as the runtime's injected watchdog). 1 ADR (0039). No schema changes beyond the quarantine reset; no new CONTEXT.md terms. |
| v0.31.0 | "Habitat State Ownership and Realtime Projection" — Restores the v0.18.3 "Single Cache" intent and completes the contract split between durable server data and ephemeral UI state. **React Query is the sole client authority** for Habitat, Column, Mission, Task, Agent, durable activity, and stats; the canonical contract matrix is `{ habitat, columns, missions }` / `{ missions, total }` / `{ mission }`, with `PublicHabitat` masking, no `board`/`feature` aliases at the UI boundary, and `PublicHabitat` (not the secret-bearing persistence shape) as the UI response type. Zustand retains only ephemeral slices: theme, presence, WIP alerts, UI selection/modal state, notifications, and a bounded `recentSSEEvents` debug buffer. **Realtime projection** is a single membership-aware event-by-representation matrix with cancel-before-patch HTTP ordering, abort/generation-safe subscription lifecycle (monotonic generation, per-generation token `AbortController`, rechecks after every await + before installing `EventSource` + inside the message handler; stale generations perform no patch, invalidate, notification, or navigation effect), and route-guarded `habitat.deleted`. **Concurrency contracts**: Mission move requires `expectedVersion` → `409 VERSION_CONFLICT` (no silent overwrite; client surfaces the conflict distinctly and invalidates to reconcile); Mission drag is single-flight per Mission, latest-target coalescing, dispatched with the settled response's canonical version, with an ephemeral overlay keyed by Mission (never a cache replacement). **Atomic Column reorder** via `POST /habitats/:habitatId/columns/reorder` (`expectedOrder`/`desiredOrder`, zero writes on conflict, post-commit `column.updated` events); the prior sequential persistence loop and any compensation requests are deleted. **Mutable-offset generation resets** for the archived infinite Query (cancelled on archive/unarchive/delete/reorder, no stable-snapshot promise). **Additive `missionSummary`** on `/habitats/:id/stats` (server-authoritative `total`/`completed`/`blocked`/`byStatus` with every status zero-filled; archived dependency Missions participate; incomplete Tasks do not). UI domain clients (`packages/ui/src/api/domains/`) match the server without alias adapters; query functions forward TanStack Query's `AbortSignal` so cancellation reaches the underlying fetch. 1 ADR (0040); no schema changes; no new CONTEXT.md terms. |
| v0.32.0 | "Task Creation Centralization" — Centralizes every Task-creation origin behind a single publication kernel: `prepareTaskPublication` → `governTaskPublication` → `publishTaskWithClient` → durable dispatch → observation gate → assignment. All 11 origins (interactive create, clone, automation, plugin, blocker clearance, workflow recovery, scheduled template/inline/handler, habitat import v3, triage cluster/orphan, manual template) now produce identical kernel artifacts: one `created`/`cloned` lifecycle event, a 6-target dispatch plan, `POST_CUTOVER` creation integrity, and a recalculation marker. Legacy raw-insert paths are **removed**. The publication state machine (`pending → published_pending_observation → created → created_unassigned`) is a one-way door enforced by CAS. Three background workers drive post-commit observation and assignment with lease fencing, single-flight guards, and starvation-safe pagination. Aggregate publishers compose the kernel for scheduled occurrences, habitat import v3 (8-domain preflight with cross-domain disposition validation), and template/inline/handler schedule shapes. Clone is prepare-edit-publish with dual SSE signal. Comprehensive cold review (6 agents, 28 findings, all CRITICAL+MAJOR fixed). Cutover flag removed — the kernel is the sole Task-creation path. |
| v0.33.0 | "Deepen: Plugin Activation Contract" — Architecture-deepening release (ADR-0041) that makes the `customHttpRoute` plugin kind's boot-time failure contract honest and bounded. **Structural faults** (duplicate `(method, path)` route; malformed/empty or unsupported method or path) are rejected at plugin **load** via a new collision-only `customHttpRouteRegistry` + adapter fields (`collisionKey`/`collisions`/`register`) added to `CATALOG` (mirrors `notificationChannel`); method is uppercased in the key (matches Fastify's case-insensitive method handling) and path is byte-equal as-declared, with `orphanCheck` field-shape + membership validation over the four supported methods so a malformed manifest is rejected per-plugin (scan continues) rather than crashing the scan. **Execution faults** (`routeHandlers` throws at mount) are made honestly crash-loud: the cosmetic `initializePlugins` try/catch+rollback is removed (it implied isolation Fastify's instance-poisoning makes impossible) and `unregisterContributions` is deleted as dead code; the boot phase is extracted into a testable `runPluginBoot` with separate non-fatal (`loadPlugins` → "continuing without plugins") and fatal (`initializePlugins` → "Plugin route initialization failed - server cannot boot" + `process.exit(1)`) regimes. The **probe / staged-runtime** for per-contribution execution-fault isolation is **deferred** after a 3-seat scope debate (the pro-expand seat, on the stronger model, conceded): the probe's core soundness property ("probe-mount success ⇒ live-mount success") is mechanically unevaluable in JS — non-idempotent mount-time side effects leak resources while looking identical to the conformance harness; ADR-0041's revisit bar is tightened to require a source-auditable plugin whose mount-time behavior is audited as side-effect-free or idempotent under re-execution. Two independent cold reviews (design + code), 5 commits, MEMORY.md pointer added. No schema changes, no route changes, no CONTEXT.md terms. |
| v0.33.1 | "Lint Cleanup" — Fixes the sole lint error (`unicorn/no-useless-fallback-in-spread` in `importAttempts.ts:1067`) and auto-fixes 265 of 373 pre-existing lint warnings via `oxlint --fix`: 119× `.sort()` → `.toSorted()`, 136× unused imports/variables removed, 5× `.reverse()` → `.toReversed()`, 4× caught-error `.cause` preserved on re-throw. Remaining 108 non-auto-fixable style warnings deferred for case-by-case treatment. No schema changes, no route changes, no ADRs, no CONTEXT.md terms. |
| v0.33.2 | "TypeScript 7.0 + Node 24 Floor" — Migrates to TypeScript 7.0 (native Go compiler, "Corsa") with zero code changes — the TS6 migration (v0.22.1) was the bridge. Raises `engines.node` from `>=22` to `>=24` to match CI. All 7 packages typecheck clean, 5612 tests pass, production binary boots on Node 24. `.d.ts` output diff shows union-member reordering only (alphabetical, no semantic change). Test execution 18% faster (32s → 26s). No schema changes, no route changes, no ADRs, no CONTEXT.md terms. |
| v0.33.3 | "E2E Smoke + Doc Sync" — TG-15 real-browser UX smoke tests (Playwright) + SPA browser-crash fix in `paths.ts`; ROADMAP/README synced to v0.33.2 delivery. No schema changes, no ADRs. |
| v0.33.4 | "Review Findings + Seed Fix" — v0.32.0 review findings filed to `docs/deferred/` + TG-17 resolved; broken seed import, dead dispatch function, missing safety check, and test-env fixes. No schema changes, no ADRs. |
| v0.33.5 | "Deepen: Shared Extracts + DORMANT Sweep" — `stableStringify`/`stableHash` (CS-57) and `substituteTokens` (CS-59) extracted to `@orcy/shared`; stale DORMANT docstrings swept across the publication kernel (CS-61, CS-65). No schema changes, no ADRs. |
| v0.33.6 | "Deepen: Naming + Import Materiality" — component prop `feature`→`mission` (CS-51), remove unused `preserveDomainTargets` (CS-58), rename legacy filenames `featureService`→`missionService` etc, add missing material fields to publication fingerprints (CS-54), close `as-never` type holes in system-origin adapters (CS-55), carry column policy + template task-level fields through v3 import (IMP-1/IMP-2/IMP-3). No schema changes, no ADRs. |
| v0.33.7 | "Fix: Mandatory Automation Rule Conditions (CS-56)" — **Blocker defect correction.** Automation Rule conditions are now **mandatory production gates**: every trigger-selected rule evaluates its stored `condition` before any action runs, with `{type:"always"}` remaining the explicit trigger-only rule. Earlier, the post-v0.20.1 cutover that activated action execution across events and scans built the evaluation context but never invoked the evaluator — so every trigger-matched rule fired its actions regardless of its stored predicate, and production `conditionResult` stayed null. Replaces the per-event/per-scan split ownership with one canonical rule-attempt lifecycle (`automationAttemptLifecycle.attemptRuleRun`) shared by event ingestion, all seven scheduled-scan families, and the manual-run route; `executeAndRecordRuleRun` is retired. Falsifying outcomes persist `status=skipped, skipReason=condition_false` with the evaluated false tree and zero action side effects; malformed/depth-exceeded conditions fail closed as `status=failed` with a bounded synthetic invalid diagnostic (no fallback to unconditional execution); the action kill switch persists `status=skipped, skipReason=disabled` with the persisted **true** `conditionResult` and zero actions; duplicate trusted `task.created` delivery owns one run and at most one action; the A→B→A causal-cycle proof yields one load-bearing causal skip and two `condition_false` skips (no phantom self-cycle labels). Hourly admission now counts admitted attempts only — rejected `rate_limited`/`cooldown`/`missing_target` rows no longer prolong the hourly lockout window. Event ingestion forwards the full `event.data` payload as `trigger.payload` so live `raw.*` conditions match simulation. Manual run (`POST /automation-rules/:id/run`) executes synchronously through the canonical lifecycle and returns the terminal run + disposition (no more stranded `running` rows); request body `{targetType?, targetId?, payload?}`. New recursive depth-bounded `AutomationConditionSchema` validates authored conditions at create/update/enable/simulation/manual and runtime-validates persisted rows (max nesting depth 5, restricted `field` roots: `task`/`mission`/`habitat`/`agent`/`sprint`/`raw`, plugin `params` passthrough retained). One terminal completion callback fires per owned `running → terminal` transition in the live process; dedupe losers, failed persistence, and concurrent finalizer losers never emit. **Operator action required before upgrade**: simulate every enabled non-`always` rule against current state to confirm intended behavior — non-`always` rules begin behaving as authored. Existing malformed/depth-exceeded conditions fail closed and surface for repair; they are **not** grandfathered into unconditional execution. No schema migration, no per-rule feature flag. |
| v0.33.8 | "Fix: Publication Kernel + Scan Tallies" — CS-53 (committed-identifier projection: `stampCommittedIdentifiersWithClient` writes the 4 committed-ID columns at the first commit point inside `publishTaskWithClient` — null `committedTaskId` broke the UI's `created_unassigned` recovery surface); CS-56 scan tallies propagated through the three standalone scan services (agent-quality, triage, orphan — primitive-copy bug zeroed all reported metrics); CS-66 stale DORMANT headers swept across the live publication kernel + tests (T11 cutover is permanent, `isCreationPublicationEnabled` always true, legacy create/clone routes removed); agent-quality threshold normalization (0-1 score × 100 vs 0-100 threshold — every adequate-sample agent was flagged degraded). No schema changes, no ADRs. |
| v0.33.9 | "Fix: Governance Freeze + Import Test + Commit Hygiene" — governance freeze quarantine state populated truthfully (was hardcoded `false`, dishonest fingerprint about interceptor admission at freeze — runtime stays authoritative for invocation gating, per-batch relaxation remains a documented gap); import scoped-delete covered on the production DB driver (FK enforcement / `ON DELETE CASCADE` verified under `foreign_keys=ON` against full habitat tree, resolving the deferred production-DB test gap); `CONTRIBUTING.md` forbids internal ticket tags in commit/PR messages (`git-cliff` regenerates CHANGELOG from subjects, leaking `CS-xx`/`IMP-x` into the public changelog). No schema changes, no ADRs. |
| v0.34.0 | "Deepen: Workflow Gate Advancement" — Architecture-deepening release (ADR-0042) that concentrates the Workflow Gate advancement transaction into one deep module (`advanceGates`) behind a single entry point. Per-gate `db.transaction` owns guarded CAS satisfaction + tx-aware audit INSERT + recovery handoff for eligible `on_fail` gates. **Fail-closed contract**: an audit or handoff INSERT failure rolls back satisfaction; source events are one-shot (no outbox, no automatic replay). Five satisfaction paths (lifecycle, pulse, automation, manual unblock, redemption) route through the one entry; a sixth (template pre-satisfied-at-attach) is a creation-time exception. A boot-only `RecoveryCoordinator` replaces the old `spawnRecoveryForGate`/`createRecoveryTask` live-config path and closes a reachable null-collapse bug (structured publication outcomes collapsed to `null` → stranded gates). `satisfiedByEventId` normalized across all six paths. Manual unblock absorbed with `workflow_gate_unblocked` preserved (CR-13/TG-1 carveout). Redemption routes through the same entry (per-gate audit + notification coexist per ADR-0035). `transition-emitter.ts` seam change forwards the real Task Event id. 1 new table (`task_recovery_handoffs`), 1 migration (0060), 1 ADR (0042). |
| v0.35.0 | "Deepen: Remote Participant Actions" — Architecture-deepening release (ADR-0043) that gives the `/api/shared/*` Remote Participant surface one transport seam (`services/tasks/remote-task-lifecycle.ts`: claim/submit/release wrappers) so a remote mutation produces the same observable lifecycle as a local one — canonical Task Event via an atomic mutation+event transaction (new `existingEventId` seam), SSE, watchers, mission recalc, subscriber hooks — closing the governance-interceptor bypass and the never-enforced Host-Approved Capability gate. Both governance and capability enforcement default ON (two-layer kill switch: `ORCY_REMOTE_GOVERNANCE_DEFAULT` env + per-habitat `remoteGovernanceSettings`). The partial-completion swallow is removed (failures reported honestly); comments reduce to `commentService.addComment`; cross-habitat denial codes are anti-probing-collapsed; a dedicated `lastActivityAt` column de-pollutes heartbeat presence from `updatedAt`; remote-creator `actorType` fidelity in the dispatch path. A local↔remote parity-contract test pins the lifecycle match. **Operator action:** approve remote-participant capabilities before upgrade (Host-Approved Capability is now enforced; empty `approvedCapabilities` refuses claims on capability-bearing tasks). 2 migrations (0061, 0062), 1 ADR (0043). |
| v0.35.1 | "Patch: Three Deferred Cleanups" — Board summary remote-actor display fidelity (RM-7), compiledStartup pnpm version pin (TG-16), and the code-evidence `actorType` cast widened to the canonical 6-value `ActorType` (CS-67). The TG-16 fix changed `execSync("pnpm …")` to `npx pnpm@9.0.0 …` so local environments (ambient pnpm v11, fnm PATH propagation) no longer break the compiled-binary smoke test — CI was already protected via explicit `corepack prepare pnpm@9.0.0 --activate`. No schema changes, no ADRs. |
| v0.35.2 | "Patch: Three Gap Closures + SSE Null-Guard" — PATCH `/api/missions/:id` now accepts `status` (closes deferred TG-15 — Zod `.strip()` was silently dropping the field, so the v0.35.0 archive e2e was a false positive that passed with no data because no data ever existed); SSE handler null-guards in `sse/registry.ts` (the lookup `SSE_EVENT_REGISTRY[type]` was throwing on unknown event types — Zustand swallowed the PAGEERROR but it polluted every habitat page-load console); `critical_path` scoring selector added (closes deferred RM-3B — longest-chain over `missionDependencies` with cycle-guarded memoized DFS; reason text: "On critical path (chain length N)"). TG-15.3 e2e uses `dispatchEvent('click')` to bypass a viewport-bound click in `Habitat.tsx:290-308`'s `overflow-x-auto` (proper layout fix deferred). 15 UI test fixture files patched for v0.35.0's `lastActivityAt` / `remoteGovernanceSettings` field additions (cleared ~150 pre-existing `TS2550` errors from the `lib: ES2025` config missing the ES2023 surface). No schema changes, no ADRs. |
| v0.35.3 | "Patch: Forward `missingCapabilities` on Delegated-Claim Failure" — closes deferred UI-1. The delegated-claim path at `routes/tasks/lifecycle.ts:74` was forwarding only `result.message` on capability_mismatch, leaving UI clients with only a message string on delegated failures (asymmetric with the local path at line 90 which already forwards the array). Both `delegateTask` and `claimDelegatedTask` in `services/tasks/task-delegation.ts` now include `missingCapabilities: missing` in the `capability_mismatch` return (return-type unions widened to `missingCapabilities?: string[]`). `claimPathCharacterization.test.ts` §8.1 pin flipped from `PRESERVE` to `INTENTIONALLY-CHANGE` and assertion strengthened to a full `toEqual` of the rich shape. New `sharedApi.test.ts` discriminating integration test. No schema changes, no ADRs. |
| v0.35.4 | "Patch: Notification write scope + linked-identity recipient dedup" — closes two deferred-roadmap items plus the stale RM-3B entry. **Notification write scope (RM-5):** the `/api/shared/notifications/deliveries/:deliveryId/ack` and `/notifications/deliveries/:deliveryId/snooze` routes were the only POSTs in the Remote MCP action manifest declared with `requiredScope: "read"`, and the server-side routes had no `remoteActionScope(...)` preHandler. Adds `"notification.write"` to `RemoteActionScope` (union + zod enum + MCP local inline union); updates `REMOTE_MCP_ACTIONS` ack/snooze entries; adds the `remoteActionScope("notification.write")` preHandler to both routes (first scope enforcement on these routes); adds the scope to both observer and contributor standing lists in `isActionAllowedForStanding`; updates the stale "read-adjacent write" test in `remote-mcp.test.ts`; adds two scope-enforcement tests in `podBridgeRemoteAuth.test.ts`. **Linked-identity recipient dedup (RM-8):** `findRemoteRecipientsForEvent` (`packages/api/src/services/remoteNotificationResolver.ts`) now uses `externalIdentityId ?? __id__:${id}` as the dedup key, so the same person/orcy across multiple pods or records receives one delivery instead of duplicates; first record wins; iteration order unchanged. Four new tests in `remoteNotificationResolver.test.ts`. **Doc cleanup:** stale RM-3B entry removed from `docs/deferred/roadmap/README.md` and `docs/deferred/README.md` (gitignored, local-only). **Operator action:** deployments with remote participants must add `"notification.write"` to any grant that should ack/snooze notifications — the routes are now scope-enforced (previously scope was declared in the manifest but not enforced server-side). No schema changes, no ADRs, no API contract additions. |
| v0.36.0 | "Deepen: Public Habitat Transport" — Architecture-deepening release (candidate 9 from the 2026-07-11 review) that establishes `PublicHabitat` as the single transport contract across server, UI, and MCP. Zod schema (`updateHabitatSchema`) is now the source of truth via `z.infer` for the service input type (widened 7→11 fields, `as` cast eliminated). All 8 settings blobs deep-merged in the service so partial PATCHes preserve existing field values. Two stale-UI SSE bugs fixed (roadmap-focus and prioritization-rules routes now broadcast `habitat.updated`). MCP aligned to `PublicHabitat` (raw `Habitat` replaced, 6-field `HabitatSettings` type-lie deleted). Dead `updateHabitatSettings` MCP tool removed (401-ed on every call — PATCH is `humanAuth`, MCP is `agentAuth`). Legacy `boardId` → `habitatId` in `HabitatClient` + `orcy_habitat` dispatch tool. MCP masking contract test (4 tests), SSE broadcast test (2 tests), compile-time drift guard. **Breaking:** MCP wire format `boardId`→`habitatId` on `orcy_habitat`; `update-settings` action removed. No schema changes, no ADRs. |
| v0.37.0 | "Deepen: Installation Transaction" — Architecture-deepening release (candidate 10 from the 2026-07-11 review) that converts the `@orcy/installer` from a descriptive append-only log into a transactional system with crash recovery. **Two-file model:** a transient journal (`install-journal.json`, presence == interrupted) + a committed manifest (`install-manifest.json`, written once at commit via temp+fsync+rename). `record()` becomes a journal-aware seam with `{path,action}` dedup + metadata upsert. **Update = re-apply intent:** `updateInstall` replays env/service/MCP/markdown/skills from a typed `InstallIntent` persisted in the manifest (was: packages + fenced-markdown only). **doctor/verify split:** doctor stays a cron-safe liveness probe (+stale-journal WARN + service status); a new read-only `verify` audits manifest↔disk drift. **Migrate hardening + v1→v2 reconcile:** `migrateLegacyInstallation` rewrites paths + is best-effort post-rename; `reconcile` upgrades v1 manifests to v2 on update. **Crash recovery:** viability-gated resume (idempotent re-run), active step-rollback (`rollbackJournal`), and a `--recover` CI flag; an unresolved `registerAgent` (POST done, credential write not) blocks resume and surfaces the orphaned agent. **Cold review + remediation:** a 3-agent independent cold review (codex/GPT-5.6) produced ~30 findings across 4 tiers; all tiers fixed (replay idempotency, data-loss paths, atomic-write durability, CLI/UX accuracy). Installer test suite 0→129 (hermetic, real `node:fs` against a temp home). **Shipped.** macOS `launchctl` state-detection and parent-dir fsync behavior warrant real-platform validation as a follow-up (not blocking). No schema changes (DB), no ADRs, no CONTEXT.md terms (installer-scoped). |
| v0.38.0 | "Learning Loop". **Shipped.** A bounded, human-governed proposal loop that converts an allowlist of trustworthy Orcy history (task/mission lifecycle audit events, terminal automation/plugin run events, terminal triage resolutions, privacy-projected experience aggregates) into immutable, cited findings. Sources are a closed allowlist (k-anonymous experience aggregates: ≥5 signals / ≥3 agents / ≥7-day coarse windows, window-scoped; all isolating fields suppressed before extractor input). Full lifecycle: scheduled + manual `ensure` + human-only reason-required `fresh_rerun` (monotonic generations) + `dry_run` + boot recovery (resumes via canonical lifecycle), all through one fenced seam with replay-safe logical work, fenced attempts, exactly-once completion, and immutable linked finding revisions (CAS review decisions with state-machine constraints). Human-only review: accept/reject/request-revision/withdraw with `expectedDecisionVersion` CAS; citation degradation (`dangling`/`changed`/`unauthorized`) blocks promotion and re-checked on every agent read. Accepted findings create at most one Habitat Wiki **draft** (never auto-publish); crash-safe idempotent promotion via deterministic tag. Agent reads via bounded `orcy_learning` MCP tool (`list_accepted`/`get`) + REST routes requiring an active Task, authorized by ONE actor-bound SQL predicate (exact Task/Mission/non-null-domain scope; no Habitat-wide fallback; collapsed denials) with citation re-resolution narrowing. Defaults OFF globally (`ORCY_LEARNING_LOOP_ENABLED`) and per-Habitat; kill switch stops new runs AND promotions; disabling does not erase accepted reads. 8 ledger tables (migration `0063`), 2 ADRs (0044 ledger/citations/lineage, 0045 authorization/privacy propagation). Deferred: plugin extractors, notification sources, non-terminal engineering findings, machine-readable automation rule drafts, automatic promotion/publication, direct Insight/Skill writes, remote participant access, event-triggered extraction, embeddings, model training, durable extraction audit-projection collector (SSE carries IDs + bounded state only; the queryable durable collector is post-v1). |
| v0.39.0 | "Habitat Shared Room". **Shipped.** Habitat co-presence labels live human and agent viewers as in the habitat. Pulse chrome states the board is shared memory. Local members may GET agent-mail bodies in the Agents drawer (ADR-0046); SSE and Notification V2 stay subject-only; humans do not send on that table. Mission **Communication** tab projects existing Pulse and comments (ADR-0047); Tasks stays default; mail stays out of that scroll. Humans can JWT-write mission comments. No schema changes. Loaded-window merge and the 50-comment list default are v1 limits. |
| v0.40.0 | "Restored Finding Triage Lifecycle". **Shipped.** Restores and deepens the per-Finding lifecycle under one command kernel (ADR-0048, superseding conflicting portions of ADR-0026/0027/0029/0033). **Identity:** the physical `triage_mission_id` column stays but the domain exposes canonical `correctiveMissionId`; bounded investigation provenance is stored separately (`admittedByTriageMissionId` + `admittedByInvestigationTaskId`). Agent routing is claim-bound (exact investigation Task's live claimant, locally or via `/api/shared` `triage.route` scope); manual activation activates the EXISTING corrective Mission (clears only the release gate). **Terminal immutability:** `resolved`/`wontfix` never reopen; recurrence requires lineage-novel evidence and creates a new row. **Stable occurrence intake:** first-writer-frozen canonical occurrences (versioned JCS/SHA-256 identity, complete prepared aggregate) so contending workers and template edits cannot duplicate or reshape publication. **Release epochs:** each Release freezes one immutable activation epoch (cap, eligible groups, exact Finding ids); activation reconciles per-Mission under locked transactions; `release.shipped` hands off to immutable Automation rule revisions through a durable fenced inbox with proof-aware `attention_required` recovery. **UI/MCP cutover:** clients send lifecycle command intents only (`/route`, `/activate`, `/resolve`, `/wontfix`); `/promote` retired (404); `insert_deferred_mission` performs exactly one route request. **Staged enforcement:** the one-shot production `migrate()` is replaced by a journal/hash/timestamp-compatible staged runner — additive watermark (migrations `0064`–`0067`), versioned preflight (v2, SHA-256 digest) with database-local attestation, then the CHECK-guarded enforcement migration `0068` (partial-unique active identity + Finding Resolution, RESTRICT evidence/corrective FKs). Blocking anomalies defer enforcement with a stable code and machine-readable report instead of bricking startup; advisory pre-cutover data never blocks. 11 migrations (`0064`–`0074`, including the post-enforcement hardening entries: automation outbox + proved-receipt CHECK, checkpoint disposition relabel, repair before-state digest, and composite evidence/epoch foreign keys with orphan archives), 1 ADR (0048), CONTEXT.md terms (Corrective Mission, Claim-Bound Triage Routing, Release Epoch). Out of scope: Cluster Resolution conversion, global Resolution uniqueness, tombstones/redaction. |
| v0.40.8 | "Patch: Webhook Delivery Failures Surface to Operators" — failed remote webhook deliveries previously wrote a failed row silently; they now publish a `webhook.delivery_failed` SSE event (UI warning toast) and enqueue an in-app notification for habitat admins with the endpoint URL and error. Jira's known-good host check tightened to port 443 only. No schema changes, no ADRs, no operator action. |
| v0.40.7 | "Patch: Jira Cloud-Only and Every Outbound Fetch Validated and Pinned" — closes the two review-filed items (#31/#32). Jira api-key connections now accept only https `*.atlassian.net` tenant URLs (a structural known-good rule replacing any need for an allowlist or auth escalation — every credentialed request provably goes to Atlassian's own servers); legacy non-Atlassian rows fail closed with a persisted sync error and reconnect instruction. Outgoing-webhook delivery (incl. test sends), Slack, and Discord posts migrate to the `fetchValidated` helper (validation + pinned resolution + fail-closed redirects + 10s), each preserving its byte-identical rejection contract; chat integrations inherit by delegation. SECURITY.md's coverage sentence is now literally true of the full set. A pre-release adversarial pass ran a bypass hunt over the host rule (userinfo/trailing-dot/punycode/port vectors all hold) and confirmed no unvalidated URL-variable fetch remains in the API; its two findings (doc staleness, missing real-helper dispatcher test) were remediated pre-release. No schema changes, no ADRs. |
| v0.40.6 | "Patch: Outbound-Fetch Hardening and Latent Plugin Watchdogs" — sweep-2026-08 follow-up, part 2 of 2 (#26/#27/#29, adversarially reviewed pre-release). `validateOutboundUrl` fails closed on empty DNS answers (a committed test had pinned the fail-open), returns its resolved IPs, and octet-bounds literal-IP detection; a new `fetchValidated` helper validates + fetches PINNED to those addresses via an undici Agent custom lookup (one resolution total, SNI verified preserved, fail-closed redirects, 10s timeout, bounded 128-agent cache) — used by the plugin webhook capability, automation `call_webhook`, and three newly-validated paths: remote webhook registration (async canonical check at create/PATCH), compact remote webhook dispatch (per-delivery), and the notification webhook channel (delivery-time; internal targets need `ORCY_SSRF_ALLOWLIST`). `notificationChannel` + `postInterceptor` gain the 30s watchdog default (latent — channel delivery unwired, post fire-and-forget; quarantine accounting unchanged per ADR-0039; manifest `timeoutMs: 0` opt-out pinned). Review follow-ups filed: four validate-then-unpinned surfaces (#31) and the unvalidated member-supplied Jira base URL (#32). |
| v0.40.5 | "Patch: Committed Pre-push Gate and Corrected Route Docs" — sweep-2026-08 follow-up, part 1 of 2 (the two zero-behavior items; the outbound-fetch SSRF hardening pair and two latent plugin watchdogs follow as the next patch). The pre-push migration gate's source is committed at `hooks/pre-push` with a one-command idempotent installer (`scripts/install-hooks.sh`, documented in CONTRIBUTING) — fresh clones previously had no gate at all; the committed hook corepack-invokes the gate and CONTRIBUTING's bare-`pnpm` snippets (the v0.40.3 push-failure template) are corepack-prefixed. Seven stale `board` response-shape comments across habitats/board-export/board-analytics routes corrected to the habitat wire keys (comment-only). No schema changes, no ADRs, no operator action. |
| v0.40.4 | "Patch: Invocation Watchdog and Canonical Webhook SSRF Validation" — sweep-2026-08 audit remediation, part 2 of 2 (the two behavior-tightening hardenings, isolated for rollback/bisect). Automation-action plugin invocations gain a 30s kind-default watchdog (timeout 0 previously disabled the watchdog entirely, so a never-settling handler blocked its invocation forever and never faulted toward quarantine); manifest `timeoutMs: 0` remains an explicit opt-out, already distinguished from omission by the `??` at effective-timeout resolution — both semantics pinned through the production dispatch seam. Plugin webhook calls and automation `call_webhook` actions switch from copy-pasted regex validation to the canonical resolution-based `validateOutboundUrl` checker (duplicated `SSRF_BLOCKED_PATTERNS` lists deleted), closing the DNS-resolves-to-private and IPv4-mapped-IPv6 bypass classes; `isPrivateIPv6` now unwraps mapped literals (dotted + hex), and webhook fetches gain 10s timeouts with fail-closed redirect handling. **Operator-visible:** webhook endpoints that redirect or exceed 10s now fail loudly; automation actions needing >30s must declare `timeoutMs`. No schema changes, no ADRs. |
| v0.40.3 | "Patch: Settings-Shape Hardening, MCP Docs Guard, Release Tooling" — sweep-2026-08 audit remediation, part 1 of 2 (six non-tightening findings; the watchdog and SSRF hardenings follow as their own patch). First-ever partial `autoAssignSettings` PATCH now backfills all six declared fields from the canonical factory (previously persisted shape-violating); `codeReviewSettings` gains a partial default (`autoApproveOnMerge: false`; its PATCH schema guarantees `taskPattern`); the registry comment states the true per-key classification (`ciCdSettings` safe by schema, `retrySettings` all-optional). Legacy partial blobs heal at the `maskSecretSettings` choke point — habitat detail/list/PATCH responses, `habitat.updated` SSE, and manifest exports all serve complete default-filled shapes (stored rows may stay partial; null stays null). New registry-driven route-response-shape suite drives the Fastify app end-to-end (mutation-proven: normalization disabled fails six of seven). MCP instructions guide de-drifted (five stale short tool names → live `orcy_habitat_*`, complete `orcy_habitat` action list, live `orcy_admin` vocabulary) and the docs-consistency guard generalized to every dispatch tool across five doc surfaces plus the embedded guide (`orcy_admin` stays deliberately unadvertised — batch actions live under `orcy_habitat_task` — encoded in the guard's existence set). Release tooling: `after:bump` and the release-notes fallback are corepack-prefixed (ends the recurring post-bump abort; validated by this release), and the local pre-push migration gate's bare `pnpm` is corepack-prefixed (untracked hook — other machines need the same one-line fix). Lint green again. No schema changes, no ADRs, no operator action. |
| v0.40.2 | "Patch: Complete Habitat Settings Shapes and Corrected MCP Docs" — review-audit remediation (three findings from PR #3 review threads). The first-ever settings PATCH on a habitat with no stored settings now merges partial input over canonical defaults (`anomaly`/`triage`/`release`/`roadmap`) with per-field nested merging — previously the partial object persisted verbatim, crashing anomaly scanning (TypeError dereferencing missing `thresholds`) and violating the declared all-required shapes for raw consumers (PATCH response, stored row, manifest export). Legacy partial anomaly blobs heal on read via per-field defaults (mirroring the release/roadmap resolvers); a class-guard test iterates every settings blob with canonical defaults. MCP docs scrubbed of the v0.36.0-removed `update-settings` habitat-dispatch action (usage-skill action table + example, INSTALL, ARCHITECTURE) with a docs-consistency guard test; the `orcy habitat update-settings` CLI command is unaffected. No schema changes, no ADRs, no operator action. |
| v0.40.1 | "Patch: Retire the Legacy Finding PATCH Adapter" — closes the v0.40.0-declared compatibility window (FU13). Every legacy `PATCH /api/triage/findings/:id` shape (no-work, link-only, unlink, mixed, terminal, target-release, unknown-field) now returns one typed `400 LEGACY_PATCH_RETIRED` with zero writes (re-fetch-proven per shape), one deprecation telemetry line, and no body parsing; auth gating retained (401 precedes). Deleted: the adapter branch + its single-write primitive `applyLegacyLinkWithClient`, `patchFindingBodySchema`, and the test-only `findingTriageService.confirmBucket/resolve/promote` seams. Characterization suites migrated to command-kernel equivalents (12 rewritten, 24 deleted with the removed behavior, 11 new retirement discriminators; net −13 tests — the surface shrank). Writer inventory corrected: `setTriageMissionId` documented TEST-ONLY with the true 20-caller/14-file test inventory; `activateGroupWithClient` described as the activation-state writer only (`routeWithClient` is the corrective-link writer). API.md PATCH section → RETIRED note; TROUBLESHOOTING operator entry added. **Breaking:** legacy PATCH clients must migrate to `/route|/activate|/resolve|/wontfix` |
| v0.41.0 | "Deepen: Authoritative HTTP Route Assembly". **Shipped.** One staged assembly now owns the production HTTP application (ADR-0049): `createHttpApplication` constructs Fastify and registers every surface (root hooks, both local API prefix groups, realtime, Remote Participant, optional UI, staged System Plugin routes), the executable holds only a narrow runtime handle, and a source-boundary guard test makes out-of-assembly route registration a review failure by construction — only the assembly imports `fastify` as a value, and no production module may acquire Node's `createRequire` loader capability from a literal `node:module`/`module` import (empty reviewed allowlist); non-literal/interpolated specifiers are the guard's documented scan residual. **Policy-installed authentication:** every route declares its effective policy through typed route config and the declaration installs the core-owned guard before route-level authorization; readiness is closed (a route without policy fails boot), `anonymous` is an explicit policy rather than an inference, and provider-signed ingress moved behind closed core verifiers that also drive exact raw-body capture. Legacy auth inference (function-name sets, public-route regexes, copied route lists) is deleted. **Plugin HTTP seam (ADR-0050, supersedes ADR-0041):** plugins declare routes in their manifest (`customHttpRoute` with `routeId`/method/relative path) and export keyed handlers; the core mounts them under `/api/v1/plugins/:pluginId/*` and the deprecated mirror with fixed local-actor auth. **Breaking (prerelease plugin SDK):** the raw `routeHandlers` Fastify callback is rejected at load with an explicit migration error. **Security containments shipped first:** Presence routes are human-authenticated and identity-bound (no anonymous join/heartbeat/leave/viewers; identity derived from the JWT, never the body), and the unverified provider-invite acceptance route (`POST /shared/invites/accept-provider`, which provisioned pods from a bare invite UUID plus caller-authored identity) is removed pending a designed state-consuming callback. Unmatched GitHub and GitLab CI webhooks now fail closed under remote posture (deferred item RA-3 resolved: zero configured secrets no longer dispatches; zero-secret local development still fail-opens). **Operator-visible:** `X-API-Version` is now wire-visible on every response and deprecated `/api` responses carry `Deprecation: true` (both were previously stamped in `onResponse` and never reached the wire — resolved deferred item RA-2); current/deprecated prefix parity is exact. No schema changes; 2 ADRs (0049, 0050). Deferred item RA-1 resolved in this implementation: Slack slash-command ingress now completes on its real `application/x-www-form-urlencoded` wire format (route-scoped urlencoded parser — parsing confined to `/chat/slack/command`, exact-byte signature verification unchanged), and the characterization probe that pinned the 500 now proves decoded-command success plus altered-byte rejection under both prefixes. |
| v0.41.1 | "Patch: HTTP Ingress Verification Hardening". **Shipped.** The HTTP assembly boundary now recognizes escaped Fastify package spellings across static imports, dynamic imports, and `require()` while preserving documented non-literal residuals. Discord interaction verification uses Node's native Ed25519 implementation with strict signature/public-key hex validation and exact timestamp-plus-body bytes; `tweetnacl` is removed. Slack wire-format characterization now pins charset-parameter acceptance and proves the form parser remains confined to the command route under both current and deprecated API prefixes. No route, authentication-policy, request, response, schema, migration, configuration, or operator-action change. |

---

## Upcoming

### Architecture Deepening (2026-08-13 Review)

All six candidates now have scoped investigation seeds in [`plans/v5/`](./plans/v5/README.md). This is a gated adoption sequence, not a promise that every candidate will ship or that the table order assigns release numbers.

| Track | Candidate | Current disposition | Entry gate and sensible release shape |
|---|---|---|---|
| Safety containment, then typed seam | [Notification Recipient Intent](./plans/v5/02-notification-recipient-intent.md) | **Strong.** Explicit-recipient-only is a plugin capability constraint, not a durable global policy. | First decide the Agent Mail audience and event/delivery atomicity findings; ship any confirmed privacy/correctness fix narrowly. After a policy ADR/grilling pass, migrate the CRITICAL enqueue/resolution hubs additively to typed recipient intent. |
| Safety containment, then retry authority | [Blind Task Retry](./plans/v5/05-blind-task-retry-execution.md) | **Strong.** Both a correctness/security repair and an architecture-deepening candidate. | First disposition manual cross-scope authority, stale unconditional resets, and retry/Recovery coexistence. Keep urgent containment separate; later introduce an atomic guarded retry command with closed outcomes and real forked-worker SQLite proof. |
| Independent structural deepening | [HTTP Route Assembly](./plans/v5/03-authoritative-http-route-assembly.md) | **Delivered** as v0.41.0 (shipped) — superseded by ADR-0049/0050 and the shipped assembly. | The Presence, provider-invite, and CI-ingress containments, policy-installed authentication, the declared plugin HTTP seam, and the authoritative assembly all landed; the deferred Slack form-encoded ingress defect (RA-1) was resolved in the same implementation cycle. |
| Lifecycle restoration | [Finding Triage Lifecycle](./plans/v5/01-finding-triage-lifecycle-restoration.md) | **Delivered** as v0.40.0 (investigation seeds superseded by the canonical plan). | Enforcement shipped in the same release through the staged runner + preflight attestation — no separate enforcement patch release is pending. |
| Orchestration deepening | [Scheduled Occurrence Orchestration](./plans/v5/04-scheduled-occurrence-orchestration.md) | **Strong, narrowed.** Centralize ownership without flattening distinct operations. | Grill explicit initial-execution and lease-recovery operations, preserve the existing deep adapters, and keep terminal operator repair distinct. Require real cross-process overlap evidence and do not bundle this with Task Retry. |
| Demand-gated decision | [Integration Provider Seam](./plans/v5/06-integration-provider-seam.md) | **Hold.** Internal provider adapters are real; the plugin contribution seam remains shallow. | No release until a concrete external provider/override consumer and superseding ADR-0028 security/product decision exist. At the stable plugin-contract milestone, retain the seam for demonstrated demand or retire the shallow contribution while preserving internal adapters. |

Adoption rules:

1. Confirmed privacy, authorization, stale-write, or atomicity repairs ship as narrow, independently discriminated patches before broad refactors.
2. Candidate 3 can be the first independent structural release after its policy gates; Candidates 1, 2, 4, and 5 require their stated grilling or re-review gates before ticketing.
3. Candidate 4 and Candidate 5 share a need for genuine cross-process tests but not a module, owner, or release.
4. Candidate 6 is not implementation backlog. Concrete demand must justify reopening ADR-0028.

---

### Future Cross-Cutting Seeds

These are intentionally unscheduled until their prerequisite foundations are in place.

| Seed | Why it waits |
|------|--------------|
| Integration OAuth/Webhook Extraction | Move OAuth flows (`*OAuth.ts`), webhook ingestion (`webhookService.ts`), and webhook creation into plugins. Requires new security-gated capabilities (`credentialManager`, `connectionWriter`), a solution to the `customHttpRoute` auth-middleware gap, and multiple ADRs. Remains held under the Candidate 6 demand gate and ADR-0028 — the in-tree adapters work; this is speculative architecture work waiting for external plugin author demand. |
