# v0.35.3 — Patch: Forward `missingCapabilities` on delegated-claim failure

A pre-existing asymmetry between the local and delegated claim paths is closed. The local path (`routes/tasks/lifecycle.ts:90`) already returned `missingCapabilities` in the 409 response body so UI clients could surface "agent lacks X capability" feedback. The delegated path was forwarding only `result.message`, leaving the array silent. The deferred doc (`docs/deferred/ui/README.md`, UI-1) called this out as a gap to close in a future fix.

Three changes:

1. **Service (`packages/api/src/services/tasks/task-delegation.ts`)** — both `delegateTask` and `claimDelegatedTask` `capability_mismatch` returns now include `missingCapabilities: missing` alongside the existing `message` field. Both return-type unions widened to `missingCapabilities?: string[]` (matches `task-lifecycle.ts:77`).

2. **Route (`packages/api/src/routes/tasks/lifecycle.ts:74`)** — the delegated path's 409 conflict now forwards `{ message, missingCapabilities }`, mirroring line 90.

3. **Tests** —
   - `claimPathCharacterization.test.ts §8.1`: PRESERVE marker flipped to INTENTIONALLY-CHANGE. Assertion strengthened from a partial "message contains X" check to a full `toEqual` of the rich shape, matching the §6.1 style. Docstring updated to document the route fix and the new contract.
   - `sharedApi.test.ts`: new `it()` block — builds a local app with the delegated-claim route + root-level error handler, mocks `claimDelegatedTask` to return `capability_mismatch`, asserts the 409 body's `missingCapabilities` matches the missing capability array.
   - `task-delegation.test.ts`: two pre-existing `toEqual` assertions (lines 96, 141) needed `missingCapabilities` added to the expected value — mechanical update.

## Verification

- Full API suite: green (`pnpm --filter @orcy/api test`). 93 passed in the touched test files; full suite 5818 passed.
- `pnpm --filter @orcy/api exec tsc --noEmit`: clean.
- `oxlint` on the touched files: 0 warnings, 0 errors.
- The 3 remaining failures (`boardSummaryService` × 2, `perfWorkflow` × 1) are pre-existing and unrelated.

## Backward compat

`missingCapabilities` is additive. The existing `message` field is preserved in the 409 response. Clients that ignore unknown fields are unaffected.

## Commits

- [`a9aefff`](https://github.com/waterworkshq/orcy/commit/a9aefff) — fix(delegation): forward missingCapabilities on delegated capability_mismatch